# nothing here for bmarks_eip

# for an attribute's value(s) there might be some variations in spelling
field_variations:
  app:
  # item is sth different. Depends on group resp.app. 
  item:

field_search_spec:
  # just dummy for debugging
  dummy:
    where: uri
    finds: uri
    how:
      re: 'https://.*'

field_search:
  # format is:
  # <some attr>:
  #   <check style>:
  #     <check value(s)>
  #   mark: <NEW_STATUS>
  #   modify:
  #     <attr>: <NEW_VALUE>
  ### or
  # <my_custom_special_case>:
  # etc.
